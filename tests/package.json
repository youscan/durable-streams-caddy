{
  "name": "durable-streams-caddy-conformance",
  "private": true,
  "description": "Conformance test harness — versions pinned in ../versions.env (CONFORMANCE_VERSION).",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@durable-streams/server-conformance-tests": "0.2.0",
    "vitest": "^4.0.0"
  }
}
